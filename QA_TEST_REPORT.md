# QZSS災危通報マップ — QAテストレポート

対象システム: みちびき(QZSS)の災危通報をリアルタイムに日本地図へ表示する
`qzss-map`(Cloud Run / eq.shum10.com)と、その受信機(Raspberry Pi、
`qzss-pi-package`)。

本レポートは、機能・回帰・セキュリティ・データ整合性・クロスブラウザ・
パフォーマンスを網羅した137件のテストケースの実行結果と、その過程で
発見・修正した12件の不具合の記録をまとめたもの。テストはローカル検証
環境で実施し、本番環境(eq.shum10.com)への破壊的操作は行っていない。

**総合判定: PASS(出荷可) — 137/137件合格、未解決の重大な不具合なし**

---

## サマリー

| テストスイート | 件数 | 結果 |
|---|---|---|
| セキュリティ(認証・XSS・不正入力) | 21 | 全合格 |
| 機能・回帰テスト(地図・通知ロジック) | 76 | 全合格 |
| 受信機・自動復旧(Python、Pi側) | 16 | 全合格 |
| データ整合性・並行処理 | 5 | 全合格 |
| クロスブラウザ(Chromium/Firefox/WebKit) | 12 | 全合格 |
| パフォーマンス・メモリリーク | 7 | 全合格 |
| **合計** | **137** | **全合格** |

## テスト環境

| 項目 | 内容 |
|---|---|
| サーバー | Node.js / Express(本番と同一コード、ローカル起動) |
| 認証 | `INGEST_TOKEN` 有効化(本番相当の認証テスト用) |
| ブラウザエンジン | Chromium / Firefox / WebKit(Safari相当) |
| 自動化基盤 | Playwright(実ブラウザ・実DOMでの検証) |
| 受信機ロジック | Python 3.14 / azarashi(実機Piで実行) |
| 対象外 | 本番環境(eq.shum10.com)への破壊的操作は実施せず |

---

## 1. セキュリティテスト(21件・全合格)

認証・入力検証・XSS耐性を検証。実際の攻撃ペイロードを注入し、DOM実行・
情報漏えい・サーバークラッシュが発生しないことを確認した。

### 1.1 サーバー側 認証・入力検証(11件)

| # | テスト項目 | 手法 | 結果 |
|---|---|---|---|
| 1 | `/ingest` 認証トークン未指定 | POST(トークンなし) | 401 想定通り |
| 2 | `/ingest` 不正な認証トークン | POST(誤トークン) | 401 想定通り |
| 3 | `/ingest` 正規トークンでの受理 | POST(正トークン) | 204 想定通り |
| 4 | 不正なJSON本体の送信 | 壊れたJSONボディ | 400・非クラッシュ |
| 5 | パストラバーサル(`/etc/passwd`) | `../../../../etc/passwd` | 404 拒否 |
| 6 | パストラバーサル(ソース窃取) | `/data/../../../server.js` | 404 拒否 |
| 7 | Discord署名検証(不正署名) | ダミーEd25519署名 | 401 拒否 |
| 8 | Discord署名検証(署名ヘッダ欠落) | 署名ヘッダなし | 401 拒否 |
| 9 | 巨大ペイロード(10万要素配列) | 大容量JSON POST | 413・非クラッシュ |
| 10-11 | 攻撃後のサーバー生存確認 | ヘルスチェック | 200 |

### 1.2 クライアント側 XSS注入(10件)

`<script>`／`onerror`／`onload`／`javascript:`／`<iframe>` 等、10種類の
攻撃ペイロードを気象地域名・震央名・火山名・河川名・Lアラート地域名の
各表示経路へ注入。全件、DOM上でスクリプトとして実行されないことを確認。

**所見**: HTML出力は `escapeHtml()` により一元的にエスケープされており、
属性値コンテキスト(class/style/data-*属性)に渡る値はすべて固定enum文字列
(内部の重大度テーブル由来)で、外部入力の生文字列が直接混入する経路が
無いことをコードレビューでも確認した。

---

## 2. 機能・回帰テスト(76 + 16件・全合格)

本セッションで実装・修正した全機能を、実データ相当のテストケースで検証。
既存機能への副作用(回帰)が無いことも合わせて確認した。

| サブスイート | 件数 | 主な検証内容 |
|---|---|---|
| タイムゾーン変換(JST表示) | 11 | UTC→JST変換、日付/年またぎ、Z付き・オフセット付きの二重変換防止、null/不正値耐性 |
| 津波情報(cat6)解除・表示ロジック | 13 | 可能性コード別の日本語文言、可能性なし(0)での自動解除、取消信号での解除、更新時の重複防止 |
| 全カテゴリ横断スモーク＋回帰 | 29 | EEW/震源/震度/津波/火山/洪水/J・Lアラートの描画、各カテゴリの解除条件、重複排除、マーカーリーク検証、不正データ耐性、気象複数地域の分離 |
| 洪水河川ジオメトリ経路 | 2 | 実在河川コードでの流路描画・警報解除時の消去 |
| 拠点地域ロック(is_in_scope) | 14 | 震度速報/気象警報/降灰の都道府県フィルタ、複数県判定、フィルタ不能カテゴリのfail-open |
| プッシュ通知(サーバー側cat6統合) | 5 | 通知対象化、本文の日本語化、重複抑制、解除後の再通知、タイトル生成 |
| TTL定数の妥当性 | 2 | cat6専用TTL(3時間)の設定値・カード生成の確認 |
| 受信監視・自動復旧(Pi側、Python) | 16 | 受信断検知、段階的復旧(USBリセット→再起動→物理対応要請)、クールダウン、復旧通知、状態遷移 |

---

## 3. データ整合性・並行処理テスト(5件・全合格)

高速な矛盾更新(発表→解除の連打等)や同時多発イベントで、内部状態が
破綻しないことを検証。

| # | テスト項目 | 結果 |
|---|---|---|
| 1 | 津波情報の発表/解除を7回連続切替 → 最終状態の整合性 | 合格 |
| 2 | 20件の震源＋気象警報を交互に高速連打 → 統合ロジックの正確性 | 合格 |
| 3 | 震源✕(GLレイヤー)件数とactiveEvents件数の一致 | 合格 |
| 4 | 気象サイトの最終更新値での正しい上書き | 合格 |
| 5 | WebSocket再接続時のマーカー重複防止 | 合格 |

---

## 4. クロスブラウザ互換性(12件・全合格)

3種のブラウザエンジン(Chromium/Firefox/WebKit=Safari相当)で同一シナリオ
を実行し、描画・ロジックの一貫性を確認した。

| ブラウザエンジン | ページ初期化 | 震源GLレイヤー描画 | JST時刻変換 | コンソールエラー |
|---|---|---|---|---|
| Chromium(Chrome/Edge相当) | 合格 | 合格 | 合格 | 0件 |
| Firefox | 合格 | 合格 | 合格 | 0件 |
| WebKit(Safari相当) | 合格 | 合格 | 合格 | 0件 |

---

## 5. パフォーマンス・メモリ(7件・全合格)

大量イベントの投入・削除を繰り返し、処理速度とメモリ・DOM要素のリークの
有無を検証した。

| 指標 | 測定結果 | 基準 | 判定 |
|---|---|---|---|
| 50件の震度速報を連続投入する処理時間 | 29ms | < 5,000ms | 合格 |
| 大量投入後のクラッシュ有無 | クラッシュなし | — | 合格 |
| 全削除後の震度バッジDOM残存数 | 0 | 0 | 合格 |
| 全削除後の震源GLレイヤー残存数 | 0 | 0 | 合格 |
| 全削除後のactiveEventsサイズ | 0 | 0 | 合格 |
| 全削除後のMapLibreマーカーDOM残存数 | 0 | 0 | 合格 |
| 3ラウンドの追加/削除後のJSヒープ増加量 | +0.0MB | < 50MB | 合格 |

---

## 6. 発見・修正した不具合一覧

本セッション中の調査・テストを通じて発見し、すべて修正・再検証済みの
不具合。重大度は実際の利用者への影響度で分類。

### DEF-01｜震源マーカーがズーム操作でずれる 【Critical】

DOMベースのマーカー(`maplibregl.Marker`)が地図キャンバスと別レイヤーで
合成されるため、複数の震源が同時に存在するとズーム中にマーカー位置が
ずれて見えた。

**修正**: 震源マーカーをDOM要素からGLシンボルレイヤー(地図と同一描画
パイプライン)へ全面移行。ズームで幾何学的にずれることが原理的に無く
なり、複数震源の重畳描画・タップでのパネル表示にも対応。
検証済み・本番反映済み。

### DEF-02｜表示時刻がUTC/JSTを取り違え、9時間ずれて表示 【Critical】

`report_time`等のタイムゾーン指定なしISO文字列をブラウザが実行環境の
ローカル時刻として誤解釈し、日本国内では実際より9時間早い時刻が表示
されていた。受信直後の情報が「何時間も前の古いデータ」に見える誤解を
招いていた。

**修正**: UTCであることを明示してから常に日本標準時(Asia/Tokyo)で表示
するよう変更。閲覧者のブラウザ設定に依存しない実装とし、地図・
レイテンシー計測ページの両方に適用。検証済み・本番反映済み。

### DEF-03｜津波情報(北西太平洋)の解除条件が仕様と不一致 【High】

「津波発生の可能性なし」を示す正式なコード値を解除条件として扱って
おらず、取消信号のみに依存していた。加えて可能性コードの日本語対応表が
実際の定義とズレていた。

**修正**: 正しい定義に基づき解除条件・日本語文言を修正(クライアント・
サーバー双方)。検証済み・本番反映済み。

### DEF-04｜津波情報のTTLが24時間で長すぎ、解除後も表示が残存 【High】

解除信号を受信し損ねたまま送信元からの再送自体が停止するケースがあり、
24時間という長い安全策TTLのため、実際には終了している情報が10時間以上
も「発表中」のまま表示され続けた。

**修正**: 他の速報性カテゴリ(気象警報等)と揃え、3時間に短縮。サーバー
側の永続状態からの復活も防止。検証済み・本番反映済み。

### DEF-05｜津波情報(北西太平洋)がプッシュ通知の対象から漏れていた 【High】

対応カテゴリを追加した際、プッシュ通知の許可カテゴリ一覧への追加が
漏れており、重要な津波情報でも通知が届かない状態だった。

**修正**: 通知対象カテゴリへ追加し、専用タイトル・本文も整備。
検証済み・本番反映済み。

### DEF-06｜拠点の地域ロックが気象警報・降灰に効いていない 【High】

地域ロック判定関数が震度速報・震源専用のフィールド名しか参照しておらず、
気象警報・降灰は別のフィールド名・コード体系のため絞り込みが一切機能
していなかった。実機で、関東限定に設定した拠点に他地方の気象警報が
素通りしていたことを確認。

**修正**: 気象警報・降灰のコード体系(都道府県コードへの単純変換が可能)
に対応する判定を追加。単純変換できないカテゴリ(EEW・洪水)は誤って
重要情報を遮断しないようフェイルオープン方針を維持。検証済み・
本番反映済み。

### DEF-07｜気象警報の複数種別が1件しか表示されない 【Medium】

同一地域に複数の災害副種別(例: 記録的短時間大雨情報＋土砂災害警戒情報)
が同時発表された場合、最も重い1件のみが見出しとして表示され、残りは
補足行に埋もれていた。

**修正**: 種別ごとに独立した見出しを重大度順に並べて表示するよう変更。
検証済み・本番反映済み。

### DEF-08｜受信断が発生しても検知・通知・自動復旧の手段が無かった 【High】

受信機のUSBシリアル接続が間欠的に切断し、緊急地震速報・震度速報・
津波警報を取りこぼす障害が発生。ハートビートの接続フラグは障害中も
正常値を返し続けるため、既存の死活監視では検知不能だった。

**修正**: 実際の受信データの有無を基準にした監視サービスを新設し、
段階的自動復旧(USB再接続→受信機再起動→長時間復旧しない場合は運用者へ
通知)を実装。検証済み・稼働中。

### DEF-09〜12｜降灰の地図非表示／通知の重複／初回訪問時の説明不足／管理コマンド未登録 【Medium】

降灰予報が地図上に一切描画されない、同一警報の再送のたびにプッシュ
通知が繰り返される、初めて利用する人向けの説明・免責事項が無い、拠点の
地域設定を変更する管理コマンドが運用チャネルに登録されていない、の4件。

**修正**: 降灰は対象範囲を楕円表示＋タップで詳細表示、通知は新規増分の
みを送信するよう変更、初回訪問時の案内ポップアップを追加、運用コマンド
を正式登録。検証済み・本番反映済み。

---

## 7. テスト対象外・既知の制限事項

- **実運用環境(衛星電波・受信ハードウェア)** — 本レポートのテストは
  ソフトウェア層を対象としており、アンテナ設置環境やUSBハードウェアの
  経年劣化など物理要因はテスト対象外。
- **本番データでの解除信号到達性** — 「解除条件のロジックが正しい
  こと」は検証済みだが、実際の解除電文が期待通りの頻度・形式で配信され
  続けるかは受信状況に依存する。
- **Discord運用コマンドのアクセス制御** — コマンド自体は登録・動作確認
  済みだが、実行権限を特定ユーザーに限定する設定は別途確認を推奨。
- **アクセシビリティ(スクリーンリーダー等)** — 本ラウンドのスコープ外。

## 8. 今後の推奨事項

1. **Discord管理コマンドの実行者制限** — 現在ギルド内の誰でも拠点操作
   コマンドを実行できる状態。特定ユーザーIDへの制限設定を推奨。
2. **受信断の統計収集** — 今回導入した監視ログを蓄積し、USBハードウェア
   の信頼性(交換要否)を定量的に判断できるようにする。
3. **北西太平洋津波情報の地図描画対応** — 現状はテキストカードのみ。
   対象沿岸が確定した段階の情報も活用するなら、海外沿岸ポイントの座標
   データ整備を検討。
4. **継続的リグレッションテスト化** — 本ラウンドで作成したテスト
   スクリプト群をCIパイプライン化し、今後のデプロイ前に自動実行する
   ことを推奨。

---

本レポートはローカル検証環境上のテスト結果に基づく。本番環境
(Cloud Run／Raspberry Pi拠点)へは、各修正の検証完了後に個別デプロイ・
OTA適用し、可能な範囲で本番実データによる事後確認も実施済み。
